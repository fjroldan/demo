pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh "mvn clean package"
            }
        }
        stage('Build') {
            steps {
                sh "mvn clean copile test"
            }
            post {
                success {
                    junit '**/target/surefire-reports/TEST-*.xml'
                    archiveArtifacts 'target/*.jar'
                }
            }
        }
    }
}
